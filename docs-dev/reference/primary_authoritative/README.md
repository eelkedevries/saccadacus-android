# primary authoritative references

Binding, canonical project material the coding agent must treat as ground truth: specifications, locked design decisions, data schemas, naming conventions, and domain rules.

Rules:
- Do not contradict anything here. If a prompt or change would conflict with it, stop and flag the conflict rather than silently overriding it.
- Keep this folder small and curated. Update or remove an entry when a decision changes — stale canon is worse than none.
- One concern per file, with a clear title. Date or version significant decisions.
- This is development reference; do not include it in the deployed build output.
