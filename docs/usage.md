# Usage

Project-facing usage instructions. Replace the placeholders below with the project's actual usage.

## Basic usage

Describe how a user interacts with the project: the main commands, screens, or entry points.

## Build

```bash
npm run build
npm run preview
```

The build output is written to `dist/` and previewed locally before deployment.
